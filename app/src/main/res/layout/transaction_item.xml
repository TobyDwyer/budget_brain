<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardBackgroundColor="@android:color/white"
    app:cardCornerRadius="8dp"
    app:cardElevation="4dp">
<androidx.constraintlayout.widget.ConstraintLayout
android:layout_width="match_parent"
android:layout_height="wrap_content"
android:padding="8dp">

<!-- Transaction Date -->
<TextView
    android:id="@+id/transactionDate"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="15 Sep 2024"
    android:textSize="14sp"
    android:textColor="#757575"
    app:layout_constraintTop_toTopOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintEnd_toEndOf="parent"/>

<!-- Transaction Amount -->
<TextView
    android:id="@+id/transactionAmount"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="$120.50"
    android:textColor="#4CAF50"
    android:textSize="18sp"
    android:textStyle="bold"
    app:layout_constraintEnd_toEndOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintTop_toBottomOf="@id/transactionDate" />

<!-- Transaction Category -->
<TextView
    android:id="@+id/transactionCategory"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="Groceries"
    android:textSize="14sp"
    android:textColor="#212121"
    app:layout_constraintTop_toBottomOf="@id/transactionAmount"
    app:layout_constraintStart_toStartOf="parent"/>

<!-- Linked Budget -->
<TextView
    android:id="@+id/linkedBudget"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="@string/hint_budget"
    android:textSize="14sp"
    android:textColor="#212121"
    app:layout_constraintTop_toBottomOf="@id/transactionCategory"
    app:layout_constraintStart_toStartOf="parent"/>

<!-- Transaction Notes -->
<TextView
    android:id="@+id/transactionNotes"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="Bought weekly groceries"
    android:textSize="12sp"
    android:textColor="#757575"
    app:layout_constraintTop_toBottomOf="@id/linkedBudget"
    app:layout_constraintStart_toStartOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.cardview.widget.CardView>
